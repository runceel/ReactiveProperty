<Window x:Class="ReactivePropertySamples.WPF.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ReactivePropertySamples.WPF.Views"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <Style TargetType="{x:Type Button}">
            <Setter Property="Margin" Value="5" />
        </Style>
    </Window.Resources>
    <StackPanel Margin="10">
        <Label Content="Samples:" Style="{StaticResource TitleLabel}" />
        <Button Content="Basic usages" Click="BasicUsagesButton_Click" />
        <Button Content="Create from objects" Click="CreateFromObjectsButton_Click" />
        <Button Content="Validation" Click="ValidationButton_Click" />
        <Button Content="ReactiveCommand" Click="ReactiveCommandButton_Click" />
        <Button Content="Collections" Click="CollectionsButton_Click" />
        <Button Content="EventToReactiveXXX" Click="EventToReactiveButton_Click" />
    </StackPanel>
</Window>
