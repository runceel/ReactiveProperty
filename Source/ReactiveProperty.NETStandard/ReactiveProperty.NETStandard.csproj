<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <PackageId>ReactiveProperty</PackageId>
    <TargetFrameworks>netstandard2.0;net8.0;net9.0;net10.0;net472</TargetFrameworks>
    <AssemblyName>ReactiveProperty</AssemblyName>
    <SignAssembly>true</SignAssembly>
    <AssemblyOriginatorKeyFile>key.snk</AssemblyOriginatorKeyFile>
    <Description>ReactiveProperty is MVVM and Asynchronous Extensions for Reactive Extensions(System.Reactive).</Description>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="System.ComponentModel.Annotations" />
    <PackageReference Include="System.Reactive" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\ReactiveProperty.Core\ReactiveProperty.Core.csproj" />
    <!-- Build the analyzer first, but do not add it as a compile-time reference. -->
    <ProjectReference Include="..\ReactiveProperty.Analyzer\ReactiveProperty.Analyzer.csproj"
                      ReferenceOutputAssembly="false"
                      PrivateAssets="all" />
  </ItemGroup>

  <ItemGroup>
    <!-- Ship the analyzer inside this package. -->
    <None Include="..\ReactiveProperty.Analyzer\bin\$(Configuration)\netstandard2.0\ReactiveProperty.Analyzer.dll"
          Pack="true"
          PackagePath="analyzers/dotnet/cs"
          Visible="false" />
  </ItemGroup>



</Project>
