<controls:ChildWindow x:Class="Silverlight.Views.ReactivePropertyBasics"
           xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
           xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
           xmlns:prism="clr-namespace:Microsoft.Practices.Prism.Interactivity"
           xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
           xmlns:controls="clr-namespace:System.Windows.Controls;assembly=System.Windows.Controls"
           xmlns:vm="clr-namespace:Silverlight.ViewModels"
           Width="auto" Height="auto"
           Title="ReactivePropertyBasics">
    <controls:ChildWindow.DataContext>
        <vm:ReactivePropertyBasicsViewModel />
    </controls:ChildWindow.DataContext>
    <StackPanel>
        <TextBlock>Appears chracter after 1 second.</TextBlock>
        <!-- ReactiveProperty binding ".Value" -->
        <TextBox Text="{Binding InputText.Value, Mode=TwoWay}">
            <i:Interaction.Behaviors>
                <prism:UpdateTextBindingOnPropertyChanged />
            </i:Interaction.Behaviors>
        </TextBox>
        <TextBlock Text="{Binding DisplayText.Value}" />
        <Button Command="{Binding ReplaceTextCommand}">Is TextBox empty or not?</Button>
    </StackPanel>
</controls:ChildWindow>