<Window x:Class="WPF.Views.ReactivePropertyBasics"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:WPF.ViewModels"
        Title="ReactivePropertyBasics" Height="300" Width="500">
    <Window.DataContext>
        <vm:ReactivePropertyBasicsViewModel />
    </Window.DataContext>
    <StackPanel>
        <TextBlock>Apperas chracter after 1 second.</TextBlock>
        <!-- ReactiveProperty binding ".Value" -->
        <TextBox Text="{Binding InputText.Value, UpdateSourceTrigger=PropertyChanged}" />
        <TextBlock Text="{Binding DisplayText.Value}" />
        <Button Command="{Binding ShowMessageBox}">Is TextBox empty or not?</Button>
    </StackPanel>
</Window>